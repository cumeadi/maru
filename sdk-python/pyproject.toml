[project]
name = "maru-sdk"
version = "0.1.0"
description = "Maru Python SDK"
dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.6.4",
    "tenacity>=8.2.3"
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
